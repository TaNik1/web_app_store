import React from 'react';
import './Product.css';

const Product = ({product, onAdd}) => {

    return (
        <div className={"produc2t"}>
            <div className={"dd"}>{product}</div>
        </div>
    );
};

export default Product;
